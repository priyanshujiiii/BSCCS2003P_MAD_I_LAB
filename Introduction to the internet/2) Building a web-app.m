Building a web app that others can use?
  
  Building a webapp involves the following steps:


1) Get a unique domain name
a .  so that people remember your webapp so that can reach it.
2) set up a computer with an IP to serve the webapp
3) link the domain name with the server's ip address.
4) write your application code(web server)
5) Deploy your application code on the computer that is your serevr host

3.1 Get a domain name

3.2 Setup a serevr 
